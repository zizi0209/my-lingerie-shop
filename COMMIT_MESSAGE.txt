feat: Nâng cấp Date Filter cho Dashboard lên Enterprise Standard

## ✨ Features mới

### Frontend Components
- DateRangePicker: Component chọn khoảng thời gian với 8 presets
  * Hôm nay, Hôm qua, 7 ngày qua, 30 ngày qua
  * Tháng này (recommended), Tháng trước, Năm nay
  * Tùy chỉnh (custom date picker)
  * Checkbox "So sánh với kỳ trước" với preview
  * Modern dropdown modal UI
  * Dark mode support
  * Responsive design

- GrowthIndicator: Component hiển thị % tăng trưởng
  * Icons: ↗ (up), ↘ (down), → (neutral)
  * Colors: Green (up), Red (down), Gray (neutral)
  * Sizes: sm, md, lg
  * Optional value display

### Backend Utilities
- dateRange.ts: Date utilities cho analytics
  * getDateRangeFromPreset(): Convert preset → date range
  * getPreviousPeriod(): Tính kỳ trước để so sánh
  * calculateGrowth(): Tính % tăng trưởng và trend
  * formatDateRangeLabel(): Format label hiển thị

## 🔄 Updates

### DashboardHome (/dashboard)
- Thay thế dropdown đơn giản bằng DateRangePicker
- Default preset: "Tháng này" (thisMonth)
- State management: dateRange + compareEnabled
- Backward compatibility: Auto convert dateRange → period

### Tracking (/dashboard/tracking)
- Áp dụng DateRangePicker tương tự
- Default preset: "7 ngày qua" (last7days)
- Tương thích với existing analytics API

## ✅ Technical Details

### Backward Compatibility
- Frontend tự động convert DateRange → period param
- Không cần sửa backend API ngay
- Logic: duration → days → period mapping

### TypeScript
- ✅ No TypeScript errors
- ✅ Proper type definitions
- ✅ No `any` types used

### Code Quality
- KISS: Simple, readable code
- DRY: Reusable components
- Single Responsibility: Each component có 1 nhiệm vụ rõ ràng
- Explicit > Implicit: Clear prop names và types

## 📊 Comparison Mode (Ready)

### Frontend
- ✅ UI components ready
- ✅ State management ready
- ✅ Preview label implemented

### Backend (Pending)
- ⏳ API cần thêm `compare` query param
- ⏳ Response cần include previous period data
- ⏳ Growth calculation logic (có sẵn trong utils)

## 📁 Files Changed

### Created
- backend/src/utils/dateRange.ts
- frontend/src/components/dashboard/DateRangePicker.tsx
- frontend/src/components/dashboard/GrowthIndicator.tsx
- DASHBOARD_DATE_FILTER_IMPLEMENTATION.md

### Modified
- frontend/src/components/dashboard/pages/DashboardHome.tsx
- frontend/src/components/dashboard/pages/Tracking.tsx

## 🎯 Impact

- **User Experience**: Modern UI với presets rõ ràng
- **Flexibility**: 8 presets + custom range
- **Scalability**: Ready cho comparison mode
- **Maintainability**: Clean, typed, reusable components

---

Tested: ✅ TypeScript compilation pass
Ready for: Production
